# 思考题与课后作业 1、2题解答

## 1. 代数证明

OLS 最小化残差平方和：
$$
Q=\sum_{i=1}^n e_i^2=\sum_{i=1}^n\left(y_i-\hat\beta_0-\hat\beta_1 x_i\right)^2
$$

对两个回归系数求偏导并令导数等于0，得到正规方程组：
$$
\begin{cases}
\displaystyle \frac{\partial Q}{\partial \hat\beta_0}
=-2\sum_{i=1}^n \big(y_i-\hat\beta_0-\hat\beta_1 x_i\big)=0 \\[6pt]
\displaystyle \frac{\partial Q}{\partial \hat\beta_1}
=-2\sum_{i=1}^n x_i\big(y_i-\hat\beta_0-\hat\beta_1 x_i\big)=0
\end{cases}
$$

### 证明 ${\sum_{i=1}^n e_i = 0}$
第一式两边除以 $-2$：
$$
\sum_{i=1}^n \big(y_i-\hat\beta_0-\hat\beta_1 x_i\big)=0
$$
由残差定义 $e_i=y_i-\hat\beta_0-\hat\beta_1 x_i$，代入得到：
$$
{\sum_{i=1}^n e_i = 0}
$$

### 证明 ${\sum_{i=1}^n x_i e_i =0}$
第二式两边除以 $-2$：
$$
\sum_{i=1}^n x_i\big(y_i-\hat\beta_0-\hat\beta_1 x_i\big)=0
$$
将残差 $e_i$ 代入：
$$
{\sum_{i=1}^n x_i e_i = 0}
$$


---

## 2. $R^2$上升，调整$R^2_{adj}$下降的原理
### 相关公式
$$
R^2 = 1-\frac{RSS}{TSS},\quad
R^2_{adj}=1-\frac{RSS/(n-k-1)}{TSS/(n-1)}
$$
- $TSS$：总平方和，仅依赖被解释变量$y$，**增加自变量不会改变TSS**
- $RSS$：残差平方和
- $n$：样本量；$k$：模型自变量个数；$n-k-1$：残差自由度

### 为什么加入纯随机无关特征，$R^2$上升或不变
$R^2$ 完全由 $RSS$ 和 $TSS$ 决定。
新增一个无关随机特征，OLS算法会利用该变量拟合样本中的随机噪声；**$RSS$不会增大，只会减小或者保持不变**。
因为 $TSS$固定，$R^2=1-\frac{RSS}{TSS}$：
1. 若$RSS$变小，$R^2$上升；
2. 若新变量与$y$完全正交，$RSS$不变，则$R^2$不变。


### 为什么 $R^2_{adj}$ 会下降
$R^2_{adj}$引入了自由度惩罚项。
增加无关特征后：
1. 自变量数量$k$增大，残差自由度 $n-k-1$ **减小**；
2. 虽然$RSS$有小幅下降，但除以更小的自由度 $n-k-1$，残差均方 $\frac{RSS}{n-k-1}$ 反而变大；
3. $\frac{TSS}{n-1}$是常数，公式中后面整个分数项变大，最终 $R^2_{adj}$ 下降。

